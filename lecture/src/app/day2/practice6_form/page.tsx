// 1) 로그인 폼에 hover, focus, dark 등 상태 실습하기
// 2) floating 라벨 실습하기

import LoginForm from "@/components/LoginForm";
import LoginFormFloating from "@/components/LoginFormFloating";

export default function Home() {
    return (
        // <LoginForm />
        <LoginFormFloating />
    );
}
